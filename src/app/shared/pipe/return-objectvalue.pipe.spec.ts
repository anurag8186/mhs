import { ReturnObjectvaluePipe } from './return-objectvalue.pipe';

describe('ReturnObjectvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnObjectvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
